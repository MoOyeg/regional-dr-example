---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vm-data-pvc
  namespace: vm-example
  labels:
    app: vm-dr-example
    appname: vm-dr-example
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ocs-storagecluster-ceph-rbd
  resources:
    requests:
      storage: 10Gi
